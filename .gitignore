# Niemals hochladen: die API-Keys
key.txt
ggkey.txt

# Generierte Daten (baut der Workflow / lokaler Lauf frisch)
deals.js
keyshop-deals.json
# genres-db.json wird bewusst committet (Worker liest sie für Genres)

# Node
node_modules/

# Lokale Overrides / Krempel
config.local.json
*.log
.DS_Store
